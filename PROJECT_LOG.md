# CarFeed Project Log

用于记录 CarFeed 在产品推进、技术决策、部署路径和成本认知上的阶段性里程碑。

## 2026-04-02: 07:50 正式抓取未见执行，07:58 的 5 条冒烟翻译首次成功

这一轮确认的重点，不再是继续猜“自动翻译是不是一次都没跑通”，而是把今天早上的真实执行结果拆开看清楚：正式工作流并没有如预期出现在 `07:50 CST`，但冒烟测试已经在 `07:58 CST` 首次成功完成并写回仓库。

### 做了什么

- 核对 GitHub Actions 运行记录
  - `Refresh Feed` 最近仍只看到 `2026-04-01 22:04 CST` 及更早的失败记录
  - 截至当前检查时，没有看到 `2026-04-02 07:50 CST` 的 `Refresh Feed` 运行记录
- 核对 `DeepSeek Smoke Test`
  - `2026-04-02 07:58 CST` 的定时冒烟测试成功执行
  - 本轮抓取 `5` 条新文章，并完成 `5` 条 DeepSeek 翻译
  - `data/automation-status.json` 已写入 `fetch_outcome=success`、`translate_outcome=success`、`build_outcome=success`
  - 翻译总量从 `100` 条增长到 `105` 条，前端池内剩余未完成翻译 `295` 条
- 继续安排一次性验证
  - 将 `translate-smoke-test.yml` 再次改为 `2026-04-02 08:25 CST`
  - 继续保持 `5` 条翻译规模，优先验证链路稳定性

### 这次说明了什么

- “一次自动翻译也没有成功” 这个判断现在已经不成立
  - 至少 `2026-04-02 07:58 CST` 这次定时 `DeepSeek Smoke Test` 已经成功
- 但“正式日常自动抓取与自动翻译已经稳定”这个判断仍然不能成立
  - 因为 `Refresh Feed` 在今天 `07:50 CST` 没有看到对应运行记录
  - 当前只能证明冒烟测试链路已跑通，不能证明正式定时链路已经正常触发

### 本轮问题判断

- 之前失败的原因
  - 不是抓取或翻译本身失败
  - 而是工作流提交阶段使用 `git add dist` 时被 `.gitignore` 拦住，导致 commit step 退出
- 这次成功的原因
  - 提交阶段已经改成 `git add -f dist`
  - 因此抓取、翻译、构建后的结果可以正常 commit 并 push
- 当前剩余问题
  - 不是翻译脚本错误，也不是 DeepSeek 配置错误
  - 而是为什么 `Refresh Feed` 没有在 `2026-04-02 07:50 CST` 出现对应的 scheduled run，这需要继续观察下一轮或手动补跑确认

### 下一步行动

1. 观察 `2026-04-02 08:25 CST` 的 `5` 条 smoke test 是否再次成功
2. 将正式 `Refresh Feed` 临时改到 `2026-04-02 08:30 CST`，继续观察是否出现新的 scheduled run
3. 继续核对 GitHub Actions 中是否出现 `Refresh Feed` 的正式定时运行
4. 若下一次 `5` 条 smoke test 仍成功，再考虑把正式工作流的可验证信息进一步增强

## 2026-04-02: 常规定时抓取改为 07:50，翻译范围收敛为仅新抓取内容

这一轮调整的核心，不是继续让 DeepSeek 去补历史 backlog，而是把日常自动化收敛成更明确的早晨节奏，只处理当天新抓进来的内容。

### 做了什么

- 调整 `.github/workflows/refresh-feed.yml`
  - 将正式定时从北京时间 `08:13 / 20:13` 改为每天 `07:50 CST`
  - 保留手动触发入口，便于临时补跑
- 调整 `scripts/prepare-translation-targets.js`
  - 新增 `--new-only` 参数
  - 当工作流传入该参数时，只把“本次新抓取且尚未完成 DeepSeek 翻译”的条目写入待翻译队列
  - backlog 未翻译条目继续保留在状态统计里，但不再进入日常自动翻译
- 更新 `README.md`
  - 同步新的抓取时间与“只翻新抓取内容”的自动化说明

### 为什么这么决策

- 之前把 backlog 一并放入日常队列，会让“今天这次自动化到底处理了什么”变得不够清晰
- 日常工作流的优先级应该是保证新增内容稳定进入中文结果，而不是把历史缺口和当天新增混在一起
- 固定到 `07:50 CST`，更符合早晨例行抓取的节奏，也更容易观察每天首轮内容更新是否正常

### 当前状态

- 正式工作流已经改为北京时间 `07:50`
- 正式工作流抓取后只会翻译本次新抓取的内容
- `data/automation-status.json` 仍会继续记录 backlog 未翻译数量，便于后续决定是否另开补历史任务

### 下一步行动

1. 观察下一次 `07:50 CST` 自动抓取是否正常生成新队列
2. 确认 `data/automation-status.json` 中 `translation_scope` 为 `new_only`
3. 如果后续需要补历史 backlog，再单独设计手动或一次性补翻任务

## 2026-04-01: 自动翻译可验证化与 21:40 冒烟测试

这一轮工作的重点，不再是继续猜“自动翻译到底有没有跑”，而是把自动抓取后的翻译链路改成可验证、可落盘、可排障的状态。

### 做了什么

- 调整 `.github/workflows/refresh-feed.yml`
  - 保留每日固定抓取节奏
  - 在正式抓取前增加 DeepSeek 配置预检
  - 抓取后先生成待翻译队列，再执行翻译
  - 运行结束后统一写入自动化状态文件
- 新增 `scripts/validate-deepseek-config.js`
  - 不再只检查 Secret 是否存在
  - 直接调用 DeepSeek 接口做最小请求，验证 API Key 和基础连通性
- 新增 `scripts/prepare-translation-targets.js`
  - 对比抓取前后的文章池
  - 优先锁定新抓取且尚未翻译的条目
  - 若当前前端池仍有 backlog，也会进入待翻译队列
- 调整 `scripts/generate-deepseek-translations.js`
  - 支持读取目标 URL 列表，只翻译指定队列
  - 空队列时会明确跳过，不再误扫全部文章
  - 缺少 `DEEPSEEK_API_KEY` 时会把失败状态写入状态文件
- 新增 `data/automation-status.json`
  - 记录抓取、翻译、构建的成功与失败
  - 记录运行前后的已翻译条数、未翻译条数和待翻译队列摘要
- 新增一次性 GitHub Actions 工作流 `.github/workflows/translate-smoke-test.yml`
  - 计划在 `2026-04-01 21:40 CST` 执行
  - 只翻译 `10` 条，用于验证“自动抓取后自动翻译”是否真实可跑

### 为什么这么决策

- 之前最大的问题不是“翻译脚本能不能本地执行”，而是自动化缺少可验证证据
- 只有把失败原因落成文件，才能避免反复出现“看起来没成功，但不知道卡在哪里”
- 先用 `10` 条定时冒烟测试验证链路，比继续盲目扩大范围更稳

### 当前状态

- 新的可验证自动化链路已经推送到 GitHub
- 正式工作流已带 DeepSeek 配置预检
- 今天额外安排了 `2026-04-01 21:40 CST` 的 `10` 条翻译冒烟测试
- 当前前端文章池中，DeepSeek 已完成 `100` 条，未完成 `300` 条

### 下一步行动

1. 观察 `2026-04-01 21:40 CST` 冒烟测试是否成功
2. 查看 `data/automation-status.json` 中的抓取、翻译和构建结果
3. 若这次 `10` 条测试通过，再恢复到日常自动增量翻译节奏继续观察
4. 再决定是否要继续补历史 backlog，还是只翻译新增内容

## 2026-04-01: 自动抓取后增量翻译并入主工作流

这一轮工作的重点，不再是单独测试一次“全量翻译能不能跑”，而是把翻译动作并入每天的自动抓取链路，验证真实日常场景下的增量自动翻译。

### 做了什么

- 重新梳理 DeepSeek 翻译需求
  - 不再把目标定义为“每天重跑全部历史内容”
  - 改为“每次自动抓取后，补翻译当天新进入的内容”
- 调整 `scripts/generate-deepseek-translations.js`
  - 默认处理范围提升到当前前端文章池
  - 运行时会先读取已有 `data/translations-deepseek.json`
  - 默认跳过已经具备 `summary_long` 的条目
  - 保留 `--force`，用于后续手动整批重跑
- 调整 GitHub Actions 工作流
  - 将 DeepSeek 翻译并入 `.github/workflows/refresh-feed.yml`
  - 自动抓取后直接执行翻译，再执行静态构建与提交
  - 提交内容扩展为 `data/translations-deepseek.json` 与 `dist`
- 删除原先一次性翻译工作流
  - 旧的 `translate-deepseek-once.yml` 属于单次试验配置
  - 不再保留为正式自动化路径
- 修正文案表现
  - 预览页弹层顶部文案改为按 `translation_source` 动态显示
  - 避免 DeepSeek 结果仍显示“免费翻译测试版”

### 为什么这么决策

- 日常新增内容量本来就不大，真正需要验证的是“增量自动翻译”能否稳定跑通，而不是反复重跑历史存量
- 把翻译动作拆成独立工作流，会让抓取、翻译、构建和发布分成多段，增加排查复杂度
- 如果翻译脚本每次都覆盖已有结果，就无法自然补齐 backlog，也不适合作为长期自动化基础
- 先把“抓取 -> 翻译 -> 构建 -> 发布”串成一条主链路，才有资格继续评估质量、成本和时延

### 当前判断

- DeepSeek 已经不再只是本地预览试验入口，而是进入自动抓取后的真实处理链路
- 当前更重要的观察项不是“能不能一次翻完 400 条”，而是每天新内容进入后，是否能自动补出稳定的中文结果
- `data/translations-free.json` 仍可以保留为本地预览回退方案，但不应继续被视为正式生产路径

### 当前状态

- 自动抓取工作流已包含 DeepSeek 增量翻译步骤
- DeepSeek 翻译脚本已具备续跑能力，不再默认从空结果开始覆盖
- 预览页优先显示 DeepSeek 结果，并根据实际来源展示对应文案

### 下一步行动

1. 将当前改动推送到 GitHub，使新的自动化配置正式生效
2. 确认仓库 Secrets 中 `DEEPSEEK_API_KEY` 已正确配置
3. 观察下一次自动抓取后，`data/translations-deepseek.json` 是否只补新增条目
4. 再评估翻译质量、处理速度和是否需要进一步限制每日翻译量

## 2026-03-30: 域名与部署路线定稿

这一轮的核心不是继续加功能，而是把项目从“本地原型”推进到“可正式上线的一级页面”。

### 做了什么

- 明确正式域名为 `carfeed.forum`
- 确认当前优先级是先把一级页上线，而不是提前做完整动态站点
- 明确部署顺序为 `DNS -> Cloudflare Pages -> 正式挂站`
- 将 DNS 接入到 Cloudflare，当前处于传播阶段
- 将部署方案收敛到 Cloudflare 免费版的静态优先路径

### 为什么这么决策

- 现阶段最重要的是先让项目拥有一个真正可访问、可展示、可验证的正式入口
- CarFeed 当前主体仍是静态内容流原型，技术形态和 Cloudflare Pages 高度匹配
- 先上线一级页，可以尽快验证品牌感、信息架构和外部访问链路
- 动态能力、二级页、个性化或后台系统都属于下一阶段问题，不应该阻塞第一次正式上线
- 先用轻量方案跑通一整条交付链路，比过早投入复杂基础设施更划算

### 放弃了哪些路线

- 不做大陆备案
  - 当前目标不是在中国大陆做合规商业化承载，备案会显著拖慢上线节奏
- 暂不进入 `Mac mini` 自建服务器
  - 自建意味着机器、网络、运维、监控、稳定性和安全责任全部提前到来
- 暂不先做动态后端
  - 现阶段没有必要为了未来能力，先引入现在并不急需的系统复杂度

### 当前 DNS 状态

- 正式域名：`carfeed.forum`
- DNS 已开始接入 Cloudflare
- 当前状态：已进入 Pages 部署联调阶段
- Pages 首次构建已触发
- 当前暴露的问题不是 DNS，而是构建流程中缺少 `build` 脚本
- 修正构建脚本后，即可继续 Cloudflare Pages 正式发布

### 成本结构

当前的成本认知已经从“能不能做”切换到“什么阶段应该配什么成本”。

- 域名成本
  - 保留正式品牌入口，属于低成本高价值支出
- Cloudflare 免费版
  - 足够承载一级静态页面上线，是当前阶段最优解
- 自建服务器成本
  - 不只是机器费用，还包括维护时间、稳定性成本和后续运维负担
- 动态系统成本
  - 一旦提前引入，就会连带带来数据库、接口、鉴权、缓存、监控等一整套复杂度

当前阶段的正确结构是：

- 低固定成本
- 低运维负担
- 快速上线
- 为后续动态扩展预留空间

### 下一步行动

1. 使用 `npm run build` 生成静态产物
2. 在 Cloudflare Pages 中将输出目录设置为 `dist`
3. 重新触发部署并确认构建通过
4. 验证正式域名访问、HTTPS、缓存和基础展示是否正常
5. 完成 CarFeed 第一阶段的公开可访问入口
6. 在此基础上再进入二级页与动态能力的设计

### 本轮最大收获

这一轮最重要的收获，不是“技术上又完成了一个动作”，而是部署思路彻底收敛了：

- 先完成品牌域名落位
- 先用静态方案拿到正式上线结果
- 不为暂时不需要的动态能力付出过早成本
- 不让基础设施焦虑打断产品节奏

这意味着 CarFeed 已经从“原型制作”开始进入“真实上线准备”阶段。

## 下一章预留

当 Pages 首次正式上线后，下一章从这里继续：

### Cloudflare Pages 正式部署一级页面

## 2026-03-31: 自动刷新与微信传播稳定性进入实施阶段

这一轮工作的重点，从“页面已上线”转向“页面能否持续更新，以及微信传播链路是否足够稳”。

### 做了什么

- 补充每日两次自动抓取与自动发布方案
  - 新增 GitHub Actions 定时任务
  - 自动抓取后提交数据并触发 Cloudflare Pages 更新
- 将前端可见数据窗口扩展为最近 `14` 天
  - 不再只输出一小段最新内容
  - 保证至少能看到一周以上的历史
- 开始修正移动端与桌面端分类选中态
  - 统一为“文字 + 深蓝色横线”的激活逻辑
- 开始处理来源 logo 失效问题
  - 不再只依赖来源站点自己的 `favicon.ico`
  - 增加通用回退图标策略
- 开始补充卡片长按分享交互
  - 优先调用系统分享
  - 回退到复制链接

### 当前判断

- 静态站点本身已经足够承载当前内容流
- “自动化抓取”不需要先上动态服务器，也可以先通过 GitHub Actions 跑通
- 微信里的 H5 外链恢复并不等于风险消失
- 如果微信是主传播场景，后续应认真评估小程序入口

### 下一步行动

1. 验证自动工作流在 GitHub 中按计划运行
2. 观察 Cloudflare Pages 是否能稳定跟随数据提交自动更新
3. 在微信环境继续观察正式域名打开情况
4. 评估是否进入小程序原生版，而不是长期只靠 H5 外链

## 2026-03-31: 首页交互、Vespa 扩展与新能源板块落地

这一轮工作的重点，从“页面能不能自动更新”继续推进到“首页交互是否更适合微信传播，以及内容结构是否开始分化出更明确的板块”。

### 做了什么

- 顶部分类改为吸顶
  - 手机端与桌面端统一为粘性分类栏
  - 滚动时分类不再随正文一起离开视区
- 调整分类选中态
  - 深蓝色横线改细
  - 进一步减少手机端视觉干扰
- 补充一级页面反馈入口
  - 右下角新增悬浮反馈浮标
  - 当前先保留空链接位，点击提示“反馈问卷链接待补充”
- 修正来源 logo
  - `The Drive` 已恢复显示
  - `Best Car Web` 改为站点实际使用的图标资源
- 手动验证抓取链路
  - 本地执行抓取后已确认 `2026-03-31` 的新内容可以进入数据文件
  - 问题不是前端没显示，而是旧数据文件未刷新
- 新增 Vespa 方向来源
  - 已接入 `Piaggio Group Press`
  - 已尝试接入 `Vespa World Club`
  - 当前稳定进入信息流的 Vespa 内容仍只有 `1` 条
- 新增新能源板块
  - 分类名确定为 `新能源`
  - 已接入：
    - `InsideEVs`
    - `Electrek`
    - `Top Gear EV`
    - `Car and Driver EV`
  - 本地抓取后 `新能源` 分类已形成独立内容量

### 当前判断

- 首页交互已经更接近可传播的一页式产品，而不是纯技术预览页
- 反馈入口应继续保留在一级页面，不应放到二级页
- 微信内不适合继续依赖浏览器系统分享 API
- Vespa 方向仍缺少第二个真正高产且可稳定自动抓取的来源
- `SIP Scootershop` 虽然内容质量高，但当前被 Cloudflare challenge 拦截，不适合作为稳定自动抓取源
- `新能源` 已经可以作为独立板块存在，不需要继续混在传统燃油内容中

### 当前状态

- 本地预览服务可通过局域网地址访问
- `2026-03-31` 新内容已能进入本地 JSON
- `Vespa` 当前可见内容：`1` 条
- `新能源` 当前可见内容：已形成大体量独立分类
- 反馈浮标已上线，但问卷链接尚未填入

### 下一步行动

1. 填入真实问卷链接，让反馈入口正式可用
2. 继续为 Vespa 寻找第二个稳定内容源
3. 评估是否为卡片增加站内 Quick Preview，而不是直接弹原网页
4. 继续观察 GitHub Actions 定时抓取在北京时间 `08:00 / 20:00` 的实际表现

## 2026-03-30: Cloudflare Pages 首次部署成功

这一章标志着 CarFeed 从“部署准备”正式进入“线上可访问”阶段。

### 做了什么

- 完成 Cloudflare Pages 项目创建
- 跑通 GitHub -> Cloudflare Pages 的自动部署链路
- 修复首次构建失败问题
  - 原因是仓库中缺少 `build` script
  - 补充了静态构建脚本后重新部署成功
- 成功生成线上预览地址
  - `carfeed.pages.dev`

### 为什么这么决策

- 当前阶段目标是先获得稳定、公开、可访问的一级页面
- 对 CarFeed 现状来说，静态部署足够支撑内容展示与外部访问验证
- 先把部署链路跑通，比继续停留在本地预览更有价值

### 本轮遇到的问题

- Cloudflare Pages 默认执行 `npm run build`
- 仓库最初没有 `build` 脚本，导致第一次构建直接失败
- 修复方式不是引入复杂框架，而是补一层最小静态构建流程
  - 输出 `dist/index.html`
  - 同步 `data/articles.json`
  - 保留现有页面结构

### 当前状态

- Pages 项目已成功部署
- 线上预览地址已可用：`carfeed.pages.dev`
- 项目已经具备“真实在线访问入口”
- 下一步重心从“能否部署”切换到“正式域名绑定与访问验证”

### 下一步行动

1. 在 Pages 项目中绑定自定义域名 `carfeed.forum`
2. 等待域名验证、证书签发和解析完全生效
3. 验证正式域名访问、HTTPS、缓存与页面加载
4. 视情况将 `www` 或 `pages.dev` 做统一跳转

### 本轮最大收获

这一轮最大的收获不是页面本身，而是发布系统已经成立：

- 本地原型不再只是本地原型
- GitHub 提交可以直接驱动线上更新
- CarFeed 已经具备第一条真实交付链路

这意味着后续一级页迭代、正式域名接入、甚至未来的二级页扩展，都已经有了一个可持续承载的上线底座。

## 2026-03-30: 正式域名激活与微信校验处理

这一阶段的重点已经从“部署能不能成功”转向“正式域名能不能稳定对外访问，以及外部平台能不能正常识别”。

### 做了什么

- `carfeed.forum` 已成功绑定到 Cloudflare Pages
- 自定义域名状态已变为 `Active`
- SSL 已启用
- 验证了正式域名下页面可访问
- 按外部平台要求部署了站点根目录验证文件
  - 文件名：`19875c79ee287a8e639c152098f54323.txt`
  - 文件内容：`797c274cd2172cf53559665df7ad13428d6e2c81`
- 已确认该验证文件可以通过正式域名直接访问

### 当前状态

- Pages 已成功上线
- 正式域名 `carfeed.forum` 已接入完成
- 域名访问和 HTTPS 已具备基础可用性
- 外部平台侧仍存在“风险提示 / 验证未通过”的问题
- 当前已进入补充说明材料与人工恢复申请阶段

### 本轮判断

- 从站点侧看，验证文件已经部署正确，路径和内容都可访问
- 当前阻塞点更可能来自平台侧缓存、校验延迟或平台风控策略
- 问题已经不再是“站点没上线”或“验证文件没放对”

### 下一步行动

1. 保持验证文件继续在线，不改文件名、不改内容
2. 等待平台侧校验刷新，必要时重新发起验证
3. 补充站点用途说明，申请恢复正常访问
4. 后续继续观察 `carfeed.forum` 在微信环境下的打开状态

### 今日收尾状态

- 产品侧：一级页面已经拥有正式线上入口
- 部署侧：GitHub -> Cloudflare Pages -> 自定义域名链路已打通
- 域名侧：`carfeed.forum` 已激活并启用 SSL
- 平台侧：微信访问风险问题尚未完全解决，留待下一轮继续处理

## 2026-04-01: 本地汉化预览、排序修正与图片抓取修复

这一轮工作的重点，不是继续直接改线上，而是先把“本地二级内容预览形态”做出来，同时修正当前信息流里已经暴露的数据质量问题。

### 做了什么

- 重新执行抓取并确认本地有 `2026-04-01` 新内容进入归档
- 修正信息流排序逻辑
  - 有图内容优先展示
  - 同组内按最新时间倒序
  - 无图内容统一沉到列表底部
- 修复 `Response.jp` 图片质量问题
  - 不再优先使用列表页裁切缩略图
  - 改为进入正文页抓主图
  - 对历史归档中的 `Response.jp` 条目一起补修
- 在本地预览页加入二级内容弹层
  - 点击卡片不再直接跳原文
  - 改为先展示站内居中弹层
  - 弹层中验证中文标题、关键词、要点和概述的内容结构
- 增加本地免费翻译实验路径
  - `preview.html` 可读取 `data/translations-free.json`
  - 若无有效结果，则在浏览器端尝试免费标题翻译并缓存到 `localStorage`
- 调整静态构建脚本
  - `npm run build` 现在会复制 `data/` 下全部 JSON
  - 为后续翻译文件进入静态产物留出通道

### 当前判断

- 首页信息流已经不应继续只是“外链集合页”
- 更合理的产品方向是：首页卡片先给中文消费层，点击后先看站内二级预览，再决定是否跳原文
- “免费翻译”适合作为本地交互验证，不适合作为正式内容生产方案
- 正式版本仍应走“抓取 -> 翻译 -> 摘要 -> 存储”的离线预计算管线
- 如果后续接入国内模型，优先级应是 `DeepSeek / 通义 / 智谱` 这类可批处理的方案，而不是继续依赖浏览器侧临时翻译

### 当前状态

- 本地抓取数据已经更新到 `2026-04-01`
- 首页顺序已调整为“有图优先 + 最新在上 + 无图置底”
- `Response.jp` 给出的低质量裁切图问题已经修复到抓取层
- 本地预览页已经能演示“中文标题 + 弹层预览”的交互方向
- 手机端预览需要通过局域网地址访问，而不是 `127.0.0.1`

### 暂未完成

- 当时还没有把 `DeepSeek` 接入正式自动化链路
- 还没有形成正式的全文汉化与摘要生产管线
- `data/translations-free.json` 当前只是测试占位，不代表已完成全量离线翻译
- 免费翻译实验还没有稳定生成高质量中文摘要

### 下一步行动

1. 在下一轮 session 中评估并接入 `DeepSeek`
2. 定义正式的翻译与摘要字段结构
3. 决定是保留免费翻译作为回退方案，还是彻底切到国内模型批处理
4. 再决定哪些内容应该进入线上，而不是继续只停留在本地预览

## 2026-04-01: DeepSeek 本地接入准备启动

这一轮不是正式跑完全量翻译，而是先把本地接入骨架搭起来，确保后续可以在不提交密钥的前提下直接试跑。

### 做了什么

- 在根目录启用 `.env` 方案，并将 `.env` 加入 `.gitignore`
- 新增 `scripts/generate-deepseek-translations.js`
  - 从本地 `.env` 读取 `DEEPSEEK_API_KEY`
  - 抽取文章标题、描述和正文段落
  - 调用 DeepSeek 生成中文标题、短摘要、长概述、要点和关键词
- 预览页调整为优先读取 `data/translations-deepseek.json`
  - 若不存在，再回退到免费翻译测试数据
- `package.json` 新增 `npm run translate:deepseek`

### 当前判断

- 这一步已经完成了“接入前置工程准备”
- 下一步核心不再是搭框架，而是实际调用 DeepSeek 并观察输出质量
- 免费翻译仍可保留为回退方案，但正式方向已经转向国内模型批处理

### 当前状态

- 本地密钥不再需要写入仓库
- 预览页已具备读取 DeepSeek 结果的能力
- 还没有实际验证本机到 DeepSeek API 的连通性与输出效果

### 下一步行动

1. 先用 `--limit 1` 或 `--limit 5` 做最小试跑
2. 观察中文标题、概述、要点和关键词格式是否符合预期
3. 再决定是否继续扩大到更多文章
