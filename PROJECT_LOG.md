# CarFeed Project Log

用于记录 CarFeed 在产品推进、技术决策、部署路径和成本认知上的阶段性里程碑。

## 2026-03-30: 域名与部署路线定稿

这一轮的核心不是继续加功能，而是把项目从“本地原型”推进到“可正式上线的一级页面”。

### 做了什么

- 明确正式域名为 `carfeed.forum`
- 确认当前优先级是先把一级页上线，而不是提前做完整动态站点
- 明确部署顺序为 `DNS -> Cloudflare Pages -> 正式挂站`
- 将 DNS 接入到 Cloudflare，当前处于传播阶段
- 将部署方案收敛到 Cloudflare 免费版的静态优先路径

### 为什么这么决策

- 现阶段最重要的是先让项目拥有一个真正可访问、可展示、可验证的正式入口
- CarFeed 当前主体仍是静态内容流原型，技术形态和 Cloudflare Pages 高度匹配
- 先上线一级页，可以尽快验证品牌感、信息架构和外部访问链路
- 动态能力、二级页、个性化或后台系统都属于下一阶段问题，不应该阻塞第一次正式上线
- 先用轻量方案跑通一整条交付链路，比过早投入复杂基础设施更划算

### 放弃了哪些路线

- 不做大陆备案
  - 当前目标不是在中国大陆做合规商业化承载，备案会显著拖慢上线节奏
- 暂不进入 `Mac mini` 自建服务器
  - 自建意味着机器、网络、运维、监控、稳定性和安全责任全部提前到来
- 暂不先做动态后端
  - 现阶段没有必要为了未来能力，先引入现在并不急需的系统复杂度

### 当前 DNS 状态

- 正式域名：`carfeed.forum`
- DNS 已开始接入 Cloudflare
- 当前状态：已进入 Pages 部署联调阶段
- Pages 首次构建已触发
- 当前暴露的问题不是 DNS，而是构建流程中缺少 `build` 脚本
- 修正构建脚本后，即可继续 Cloudflare Pages 正式发布

### 成本结构

当前的成本认知已经从“能不能做”切换到“什么阶段应该配什么成本”。

- 域名成本
  - 保留正式品牌入口，属于低成本高价值支出
- Cloudflare 免费版
  - 足够承载一级静态页面上线，是当前阶段最优解
- 自建服务器成本
  - 不只是机器费用，还包括维护时间、稳定性成本和后续运维负担
- 动态系统成本
  - 一旦提前引入，就会连带带来数据库、接口、鉴权、缓存、监控等一整套复杂度

当前阶段的正确结构是：

- 低固定成本
- 低运维负担
- 快速上线
- 为后续动态扩展预留空间

### 下一步行动

1. 使用 `npm run build` 生成静态产物
2. 在 Cloudflare Pages 中将输出目录设置为 `dist`
3. 重新触发部署并确认构建通过
4. 验证正式域名访问、HTTPS、缓存和基础展示是否正常
5. 完成 CarFeed 第一阶段的公开可访问入口
6. 在此基础上再进入二级页与动态能力的设计

### 本轮最大收获

这一轮最重要的收获，不是“技术上又完成了一个动作”，而是部署思路彻底收敛了：

- 先完成品牌域名落位
- 先用静态方案拿到正式上线结果
- 不为暂时不需要的动态能力付出过早成本
- 不让基础设施焦虑打断产品节奏

这意味着 CarFeed 已经从“原型制作”开始进入“真实上线准备”阶段。

## 下一章预留

当 Pages 首次正式上线后，下一章从这里继续：

### Cloudflare Pages 正式部署一级页面

## 2026-03-30: Cloudflare Pages 首次部署成功

这一章标志着 CarFeed 从“部署准备”正式进入“线上可访问”阶段。

### 做了什么

- 完成 Cloudflare Pages 项目创建
- 跑通 GitHub -> Cloudflare Pages 的自动部署链路
- 修复首次构建失败问题
  - 原因是仓库中缺少 `build` script
  - 补充了静态构建脚本后重新部署成功
- 成功生成线上预览地址
  - `carfeed.pages.dev`

### 为什么这么决策

- 当前阶段目标是先获得稳定、公开、可访问的一级页面
- 对 CarFeed 现状来说，静态部署足够支撑内容展示与外部访问验证
- 先把部署链路跑通，比继续停留在本地预览更有价值

### 本轮遇到的问题

- Cloudflare Pages 默认执行 `npm run build`
- 仓库最初没有 `build` 脚本，导致第一次构建直接失败
- 修复方式不是引入复杂框架，而是补一层最小静态构建流程
  - 输出 `dist/index.html`
  - 同步 `data/articles.json`
  - 保留现有页面结构

### 当前状态

- Pages 项目已成功部署
- 线上预览地址已可用：`carfeed.pages.dev`
- 项目已经具备“真实在线访问入口”
- 下一步重心从“能否部署”切换到“正式域名绑定与访问验证”

### 下一步行动

1. 在 Pages 项目中绑定自定义域名 `carfeed.forum`
2. 等待域名验证、证书签发和解析完全生效
3. 验证正式域名访问、HTTPS、缓存与页面加载
4. 视情况将 `www` 或 `pages.dev` 做统一跳转

### 本轮最大收获

这一轮最大的收获不是页面本身，而是发布系统已经成立：

- 本地原型不再只是本地原型
- GitHub 提交可以直接驱动线上更新
- CarFeed 已经具备第一条真实交付链路

这意味着后续一级页迭代、正式域名接入、甚至未来的二级页扩展，都已经有了一个可持续承载的上线底座。
